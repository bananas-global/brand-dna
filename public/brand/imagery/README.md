# Imagery assets

Place the visual references used by the Imagery guideline in this folder.

Each file is linked from `imagery.directions[].asset` in `../brand-dna.json`. Keep titles, descriptions, and reusable generation prompts in that JSON entry so the published guideline and editor remain synchronized.

Supported formats: AVIF, JPEG, PNG, SVG, and WebP.
