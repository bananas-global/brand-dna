# Logo assets

Replace the SVG files in this folder while keeping their filenames:

- `default.svg` — primary logo
- `icon.svg` — square icon
- `wordmark.svg` — wordmark without the icon
- `black.svg` — one-color black logo
- `white.svg` — one-color white logo

Keep each SVG tightly cropped to its artwork and preserve its native proportions.
